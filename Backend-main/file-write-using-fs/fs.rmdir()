const fs = require('fs');

fs.rmdir("./copy", { recursive: true }, function(err) {
    if (err) {
        console.log(err); 
    } else {
        console.log("removed"); 
    }
});


//Output:- removed and delete the copy folder
